/** @format */

// tạo nhưng thành phần const cho http code , error message
const STATUS_CODE = {
   SUCCESS: 200,
   CREATED: 201,
   BAD_REQUEST: 400,
   NOT_FOUNDED: 404,
   ERROR_SERVER: 500,
};

module.exports = STATUS_CODE;
